Models

This folder contains trained deep learning models used for handwritten digit recognition.

Models

model.h5

Predicts handwritten digits (0–9)

Algorithm: Convolutional Neural Network (CNN) trained on MNIST

 Model files may be ignored in GitHub using .gitignore.
To generate the model, run the training script provided in the src/ folder (or use a pre-trained MNIST CNN).
